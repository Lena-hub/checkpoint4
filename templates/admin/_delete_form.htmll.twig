<div> <form class="deletebutton" method="post" action="{{ path('menu_delete', {'id': menu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menu.id) }}">
    <input type="hidden" name="_method" value="DELETE">
        <button class="btnSupp">Supprimer</button>
</form> </div>
